# --- Secrets / certs : ne jamais envoyer de clés privées ---
certs/**/private/**
certs/**/*.key
certs/**/tls.key
certs/**/tls.*.dec

# Autoriser uniquement les bundles CA (utiles dans les Dockerfile)
!certs/**/ca.crt
!certs/**/ca-bundle.crt
!certs/**/bundle.crt
# (optionnel) garder la clé chiffrée si tu en as besoin
!certs/**/tls.key.enc

# --- Caches Node / Front (valable dans n'importe quel sous-dossier) ---
node_modules
.pnpm-store
.yarn
.next
dist

# --- Caches Python / Back ---
__pycache__
.venv
.mypy_cache
.pytest_cache

# --- VCS / OS ---
.git
.gitignore
.DS_Store
Thumbs.db
